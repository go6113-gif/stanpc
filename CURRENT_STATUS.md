# StanPC 프로젝트 현황 보고서

**작성일**: 2026-08-14
**조사 방법**: 실제 코드 파일 열람, Git 변경 이력, 타입 검사(코드 문법 오류 검사) 실행, 데이터베이스 접속 시도 결과를 기준으로 작성. 추정이나 계획 문서가 아닌, 지금 이 순간 `D:\StanPC` 폴더에 있는 실제 코드만 근거로 삼았습니다.
**개발 중단 여부**: 이 보고서 작성 중 신규 코드 수정은 하지 않았습니다.

---

## 요약 (3줄)

1. **화면(프론트엔드)은 대부분 실제로 작동합니다.** 메인·탐색·도감(Wiki)·마이볼트·카드 상세 페이지 모두 진짜 데이터베이스에서 데이터를 가져와 화면에 그립니다. 다만 **로그인 버튼은 눌러도 실제로 로그인이 되지 않습니다** (아래 4-4 참고, 가장 시급).
2. **서버/데이터 파이프라인도 뼈대는 다 갖춰져 있습니다.** API 30개 중 23개가 실제 DB를 조회하고, 검색엔진용 자동 페이지 생성(sitemap) 코드도 실제로 DB를 읽어 XML을 만듭니다.
3. **딱 하나, "정확히 몇 건이 저장돼 있는지"만 이번 조사에서 확인하지 못했습니다.** 조사 환경(이 자동화 도구)이 실제 운영 데이터베이스 서버로 나가는 네트워크 자체가 막혀 있어서입니다 (코드 문제 아님 — 아래 4-1 참고). 대표님 컴퓨터에서 명령어 한 줄만 실행하면 바로 확인 가능하며, 아래에 그 명령어를 적어드렸습니다.

---

## 1. 현재 전체 완성도

| 구분 | 상태 | 근거 |
|---|---|---|
| **화면단 (프론트엔드)** | 실제 작동 중 (약 50개 페이지·경로) | `app/` 폴더 안에 실제로 존재하는 화면 파일 50개 확인. 목록은 2번 항목 참고 |
| **서버단 (백엔드 API)** | 실제 작동 중 (30개 중 23개가 DB 연결) | API 파일 30개 중 23개가 실제 데이터베이스 조회 코드를 담고 있음 |
| **코드 문법 오류** | **0건** | `타입 검사`(코드에 실수가 없는지 자동으로 확인하는 절차)를 전체 프로젝트에 돌려본 결과 오류 없음 |
| **로그인(소셜 가입)** | **코드는 있으나 실제로는 작동 안 함** | 구글/트위터/카카오/네이버 로그인에 필요한 "인증키"가 전부 임시값(더미)으로 되어 있음. 화면엔 로그인 버튼이 뜨지만 눌러도 실패함 |
| **데이터베이스 실제 저장 건수** | **이번 조사에서 미확인** | 조사 도구가 운영 DB 서버(Supabase)에 접속하지 못함(네트워크 차단). 대표님 컴퓨터에서는 정상적으로 개발이 이뤄지고 있었으므로, 로컬에서 직접 실행하면 확인 가능 (4-1 참고) |

**한눈에 보는 완성도**: 화면과 서버 코드 자체는 "다 지어놓은 건물"에 가깝습니다. 다만 "정문 출입증 발급기(로그인)"가 아직 진짜 카드를 인식하지 못하는 상태이고, "창고에 물건이 몇 개 있는지"는 이번엔 창고 문이 잠겨 있어 세지 못했습니다.

---

## 2. 지금 실제로 작동하는 것 (Working Features)

### 브라우저에서 직접 누르고 볼 수 있는 화면

| 경로 | 기능 | 실제 데이터 연결 |
|---|---|---|
| `/` (메인) | 인기 포토카드 100장을 고밀도 그리드로 노출 | ✅ DB에서 실제 조회 |
| `/gallery` | 포토카드 전체 탐색 + 필터(그룹/멤버/앨범 등) + 검색 | ✅ DB에서 실제 조회, 로그인 여부에 따라 개인화 |
| `/wiki` | "덕후 Wiki" — 그룹/멤버/카드 검색, 인기 랭킹 | ✅ 실시간 검색 API 연동 |
| `/wiki/[group]/[member]` `/wiki/[group]/[member]/[album]` | 멤버·앨범별 포토카드 상세 도감 페이지 (검색엔진 최적화용 자동 생성 페이지) | ✅ |
| `/card/[cardSlug]` | 카드 1장의 상세 페이지 (가격, 통계, AI 작성 가이드 글) | ✅ 오늘(8/14) 마지막으로 수정되어 실제 연결 완료 |
| `/vault` | "마이볼트" — 내 컬렉션 관리 화면 (보유/원함/거래용/완성세트 통계) | ✅ 단, 로그인이 안 되므로 실제 진입은 막혀 있음 |
| `/vault/[username]`, `/u/[username]` | 공개 프로필(자랑) 페이지 | ⚠️ **동일한 기능이 두 경로에 중복 구현**되어 있음 — `/u/`가 더 최근(8/13) 수정본. 둘 중 하나로 정리 필요 |
| `/card-generator` | SNS 자랑용 카드 이미지 생성기 | ✅ |
| `sitemap`, `robots.txt` | 검색엔진에 페이지 목록을 자동으로 알려주는 파일 | ✅ 실제 DB를 읽어 24시간마다 자동 갱신 |

### 정상적으로 데이터를 가져오는 서버 기능 (API)

- 전체 API 30개 중 **23개가 실제 데이터베이스에 연결**되어 있음 (그룹/멤버/앨범/카드 검색, 가격 이력, 마이볼트, 아웃바운드 클릭 추적 등)
- 나머지 7개는 로그인 처리, SNS 공유용 이미지 생성, eBay 실시간 검색 등 DB가 아닌 다른 방식으로 동작 (정상)
- eBay 실시간 매물 검색(`/api/ebay/search`)은 저장된 데이터가 없어도 매번 실시간으로 eBay에 직접 질의하도록 구현되어 있음

---

## 3. 작업하다가 멈춘 곳 (Incomplete / Work in Progress)

### 3-1. 카드 상세 화면의 "탭" 기능 — 절반만 연결됨

카드 1장을 눌렀을 때 나오는 정보 탭(가이드/가격/버전/수집가)이 **화면 종류에 따라 완성도가 다릅니다**:

| 탭 | 카드 상세 페이지(`/card/[cardSlug]`)에서 | 팝업 모달(리스트에서 카드 클릭 시)에서 |
|---|---|---|
| 가이드 (AI가 쓴 카드 설명) | ✅ 오늘 실제 연결 완료 | ✅ |
| **가격** | ⚠️ 추정가·원함/보유 숫자만 표시 (구버전) | ✅ 최근 30일 시세 그래프, eBay 실시간 매물, 여러 마켓 비교까지 포함한 **새 버전**이 어제 완성됨 |
| 버전 / 수집가 | "콘텐츠 준비 중" 표시만 있음 | "Coming Soon" 표시만 있음 |

**쉽게 말해**: 가격 탭의 "고급 버전"(그래프, 실시간 매물 등)을 어제 새로 만들었는데, 아직 팝업 모달에만 붙였고 카드 상세 페이지 쪽에는 옮겨 붙이지 못한 상태입니다. 나머지 작업 중 가장 먼저 마무리하면 좋은 항목입니다.

### 3-2. 새 시세 수집 파이프라인 — 코드만 있고 한 번도 실행 안 됨

`scripts/dual_source_pilot.py`, `dual_source_pilot_executable.py`, `dual_source_image_pipeline.py` (어제 밤 작성, 총 3개 파일)가 있지만, 이 프로그램들이 만들어야 할 결과 파일(`dual_source_report.json`, `pilot_report_template.json` 등)이 실제로는 **어디에도 존재하지 않습니다.** 즉 설계도는 그렸지만 아직 시운전을 해보지 않은 상태입니다.

### 3-3. 개발자 전용 테스트 화면이 남아 있음

`app/dev-modal-preview/` — 카드 상세 팝업을 미리 테스트해보기 위해 만든 화면으로, 코드 주석에 "어디에도 연결되어 있지 않고, 삭제해도 안전함"이라고 직접 적혀 있습니다. 실제 서비스 화면이 아니라 개발자 확인용입니다.

### 3-4. "그룹(Group)"을 "아티스트(Artist)"로 바꾸는 작업이 진행 중

지금 스키마(데이터베이스 설계도)에는 그룹 정보를 담는 표가 두 개(`Group`, `Artist`) 있습니다. 기존 `Group`은 그룹 전용이라 솔로 가수를 표현하지 못해서, 이를 대체할 `Artist` 표를 새로 만들어 병행 운영 중입니다. 코드 주석에 "22개 파일이 아직 옛날 방식(Group)을 쓰고 있어서 한 번에 이름만 바꾸는 건 위험하다"고 명시되어 있어, 계획된 전환 작업이며 아직 완료 전 단계입니다.

---

## 4. 핵심 자산 및 엔진 상태 점검

### 4-1. 데이터베이스 실제 적재 건수 — **이번 조사에서 확인 불가 (중요)**

이번 자동 조사 도구가 실행되는 환경에서 운영 데이터베이스(Supabase, 실제 서비스가 쓰는 서버)로 나가는 인터넷 연결 자체가 차단되어 있어(`DNS 조회 실패`), 접속 시도가 전부 실패했습니다. 이는 **코드나 데이터의 문제가 아니라, 이 조사 도구가 실행된 환경의 네트워크 제약**입니다 — 평소 개발하시던 컴퓨터에서는 정상적으로 접속됩니다.

**대표님 컴퓨터(또는 개발자)에서 아래 명령어를 `poca-exchange` 폴더 안에서 실행하시면, 그룹/멤버/앨범/카드가 정확히 몇 건 들어있는지 30초 안에 확인하실 수 있습니다:**

```bash
npx prisma studio
```
(브라우저 창이 열리며 표 형태로 모든 데이터를 직접 눈으로 확인 가능)

참고할 수 있는 간접 근거는 있습니다: 데이터를 넣는 데 쓰이는 원본 파일이 `data/` 폴더에 있고(그룹 931개·앨범 6,646개·카드 3,860건 규모의 CSV), 데이터베이스 구조 변경 이력(마이그레이션)이 8월 10일부터 오늘(8/14)까지 총 10차례 있었으며, 가장 최근 변경이 오늘 새벽 "가이드 글 저장 칸 추가"였습니다. 즉 데이터 적재 작업이 최근까지 계속 진행되어 온 정황은 뚜렷합니다.

### 4-2. pSEO (검색엔진 자동 최적화) — 정상 작동

- `lib/seo-config.ts`(270줄), `lib/seo-generator.ts`(100줄) — 카드 1장, 그룹 1개, 멤버 1명마다 자동으로 붙는 검색엔진용 제목·설명 문구를 만드는 코드. 실제 파일로 존재하며 페이지들이 이 코드를 갖다 씀
- Sitemap(검색엔진에 "이런 페이지들이 있어요"라고 알려주는 파일) 자동 생성 코드 4개(`groups.xml`, `albums.xml`, `static.xml`, `xml`)가 전부 실제 DB를 조회해서 XML을 만들도록 되어 있음. 24시간마다 자동 갱신
- **주의**: 예전 기록(메모리)엔 이 파일들이 `app/sitemap/`에 있다고 되어 있었는데, 실제로는 `app/api/sitemap/`에 위치가 정리되어 있었습니다. 기능은 동일하게 작동하니 문제는 아니지만, 문서상 위치 정보는 업데이트가 필요합니다.

### 4-3. GEO (AI 검색엔진 대응) — 오늘 막 연결 완료

- `components/card-detail/guide-markdown.tsx` — AI(Claude)가 작성한 카드 설명 글을, 검색엔진/챗GPT 같은 AI가 바로 읽어갈 수 있는 형태(서버에서 미리 완성된 HTML)로 변환하는 코드
- 오늘(8/14) 13:56, 이 기능이 카드 상세 페이지의 "가이드" 탭에 실제로 연결 완료됨 (아래 5번 항목 참고)
- 다만 카드마다 이 "AI 설명 글"이 실제로 몇 건이나 채워져 있는지는 4-1과 같은 이유로 이번엔 확인하지 못했습니다

### 4-4. 로그인 기능 — **작동 안 함 (가장 시급한 항목)**

`.env` 설정 파일을 확인한 결과, 구글·트위터·카카오·네이버 로그인에 필요한 인증키(비밀번호 같은 것)가 전부 2글자짜리 임시값으로 채워져 있습니다. 진짜 키는 보통 수십 자입니다. 코드는 "키가 비어있지 않으면 그 로그인 버튼을 화면에 띄운다"는 방식이라, **화면에는 로그인 버튼이 정상적으로 보이지만, 실제로 누르면 인증 단계에서 실패**합니다.

→ 마이볼트, 리액션, 명예의 전당 등 로그인이 필요한 모든 기능이 지금은 "화면은 있지만 아무도 실제로 못 쓰는" 상태입니다. 각 소셜 플랫폼(구글/카카오/네이버/X)에서 개발자 앱 등록 후 발급받은 진짜 키를 `.env`에 넣는 작업이 필요하며, 코드 수정은 필요 없습니다.

### 4-5. eBay 시세 수집 — 부분 작동

- `EBAY_CLIENT_ID`, `EBAY_CLIENT_SECRET`는 실제 값으로 설정되어 있어 정상 작동 가능한 상태
- 8월 12일에 수집된 eBay 매물 데이터(약 8.9MB), 네이버 데이터(약 4.8MB)가 `data/` 폴더에 실재함
- 다만 이후 만든 "듀얼소스(eBay+네이버 통합) 파이프라인"은 3-2에서 설명한 대로 아직 실행 전

---

## 5. 정확한 작업 중단 지점 (Code Freeze Point)

가장 마지막으로 손을 댄 실제 작업 파일과 시각(파일 수정 시각 기준):

| 시각 (8/14) | 파일 | 작업 내용 |
|---|---|---|
| **13:56:14** (최종) | `app/card/[cardSlug]/page.tsx` | 카드 상세 페이지에 `GuideMarkdown`(AI 설명 글 렌더러) 연결 |
| 13:56:07 | `components/card-detail/card-tabs.tsx` | 가이드 탭에 실제 콘텐츠가 들어가도록 `guideContent` 속성 배선 |
| 13:55:53 | `components/card-detail/guide-markdown.tsx` (신규 파일) | AI가 쓴 마크다운 글을 서버에서 HTML로 미리 변환하는 컴포넌트 신규 작성 |
| 13:54:28 | `prisma/schema.prisma` | 카드 표에 `guide_content`(AI 설명 글 저장 칸) 필드 추가 |
| 09:03 | `scripts/rank_all_groups.py`, `data/group-rankings.json` 등 | 그룹 랭킹 산출 스크립트/데이터 |
| 00:50 | `components/modal/PhotocardDetailModal.tsx`, `Tab2_Price.tsx` | 카드 팝업의 "가격" 탭에 시세 그래프·실시간 매물 기능 새로 연결 |

**결론**: 지금 이 순간 코딩을 멈춘다면, 가장 마지막으로 만지고 있던 것은 **"AI가 자동으로 써주는 카드 설명 글(GEO 콘텐츠)을 카드 상세 페이지에 실제로 노출시키는 작업"**이며, 이는 [app/card/[cardSlug]/page.tsx:166-168](poca-exchange/app/card/%5BcardSlug%5D/page.tsx)에서 막 끝난 참입니다. 아직 커밋(저장 확정)되지 않은 변경사항이므로, git 이력에는 아직 기록되지 않은 "작업 중" 상태입니다.

---

## 다음 확인 시 대표님이 직접 해보실 수 있는 것

1. `poca-exchange` 폴더에서 `npx prisma studio` 실행 → 데이터 실제 건수 눈으로 확인
2. `.env` 파일에 구글/카카오/네이버 로그인 진짜 키 발급받아 입력 → 로그인 기능 살아남
3. `npm run dev` 실행 후 브라우저에서 `/card/[존재하는카드슬러그]` 접속 → 오늘 완성된 AI 설명 글 탭 확인

---
*본 보고서는 2026-08-14 기준 코드 스캔 결과이며, 이후 작업으로 내용이 달라질 수 있습니다.*
